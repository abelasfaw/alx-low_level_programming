/**
 * _abs- computes absolute value of a number
 * @n: input
 * Return: return absolute value
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)

	{
		return (-n);
	}
	else
	{
		return (0);
	}
}

/**
 * _islower- checks if character is lowercase
 * @c: type int input to be checked
 * Return: returns 1 if character is lowercase, else returns 0
 */
int _islower(int c)
{
	if (c > 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
